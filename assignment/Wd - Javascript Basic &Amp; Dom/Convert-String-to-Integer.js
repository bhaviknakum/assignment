// How can you convert the string of any base to an integer in JavaScript?

// In JavaScript, you can use the parseInt() function to convert a string of any base to an integer. The parseInt() function takes two arguments: the string to be parsed and the base of the string.

const decimalString = '10';
const decimal = parseInt(decimalString, 10); // 10

console.log(decimal)